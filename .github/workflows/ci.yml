name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore Core/CosmoApiServer.Core.csproj

      - name: Build
        run: dotnet build Core/CosmoApiServer.Core.csproj --no-restore -c Release

      - name: Test
        run: dotnet test tests/CosmoApiServer.Core.Tests.csproj --no-build -c Release
